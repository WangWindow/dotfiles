{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Cmake Debug(lldb)",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
